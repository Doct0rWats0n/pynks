from pygame.sprite import Group

SIZE = WIDTH, HEIGHT = (800, 600)

all_sprites = Group()
tank_layout = Group()
bullet_layout = Group()
wall_layout = Group()

ui_layout = Group()

MIN_CELL_SIZE = 10
MAX_CELL_SIZE = 100
